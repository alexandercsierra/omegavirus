class Room:
    def __init__(self, code, player_codes, location, is_trapped, items, color):
        self.code=code
        self.player_codes = player_codes
        self.location = location
        self.is_trapped = is_trapped
        self.items = items
        self.color = color 

#room
    #code
    #code of each player
    #location on board?
    #is booby trapped?
        #correct code
    #inventory
    #color (key it needs for entrance)